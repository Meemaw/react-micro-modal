language: node_js

node_js:
  - '8'
  - '10'
  - '11'

script:
  - echo "Running tests against $(node -v) ..."
  - yarn test

jobs:
  include:
    - stage: release
      node_js: lts/*
      deploy:
        provider: script
        skip_cleanup: true
        script: npx semantic-release
